experiment_name: "coduar_eval_placeholder"

model:
  checkpoint: "/path/to/coduar_checkpoint.pth"
  config: "configs/train_coduar.example.yaml"

evaluation:
  split: "test"
  batch_size: TODO
  report_per_element_metrics: true
  report_whole_action_metrics: true
  use_temporal_refinement: true
  temporal_refinement:
    method: "pmi_or_sequence_refinement_placeholder"
    weight: TODO

paths:
  include: "configs/paths.local.yaml"
  output_dir: "outputs/evaluation/${experiment_name}"
